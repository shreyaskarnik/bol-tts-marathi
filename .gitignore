__pycache__/
*.py[cod]
*.egg-info/
.venv/
.python-version-local
.DS_Store

# Training artifacts produced by scripts
training/train_list.txt
training/val_list.txt
training/rasa_mr.txt
training/indicvoices_r_mr.txt
training/indicvoices_r_mr_stats.json
training/indictts_mr_stats.json
training/speaker_map.json
training/kokoro_base.pth
training/config.json
training/*.full.txt
training/logs/

# Local dataset (never commit audio)
dataset/

# Checkpoints + voicepacks — too large + change often
checkpoints/
voices/
logs/
test_output*/

# HF push staging — keep the model-card template (hf_model/README.md); the rest
# (weights, voicepacks, duplicated configs) gets regenerated by scripts/push_to_hf.sh.
hf_model/kokoro-mr-v1_0.pth
hf_model/config.json
hf_model/voice_speeds.json
hf_model/voices/

# Local build bundles
upload/*.tar.gz
*.tar.gz
CHECKSUMS.txt

# Auto-monitor logs
*.log
*.out

# Editor / OS
.idea/
.vscode/
*.swp
